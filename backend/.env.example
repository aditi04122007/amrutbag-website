# Server Port
PORT=5000

# Authentication
JWT_SECRET=supersecretjwtkey_amrutbag_2026

# Optional: External MySQL Configuration (if omitted or unreachable, built-in fallback storage is used automatically!)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=mysql
DB_NAME=bagstore
